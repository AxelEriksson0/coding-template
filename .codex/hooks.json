{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "^(apply_patch|Edit|Write)$",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'pnpm lint'",
            "timeout": 120,
            "statusMessage": "Running lint"
          },
          {
            "type": "command",
            "command": "bash -lc 'pnpm build'",
            "timeout": 300,
            "statusMessage": "Running build"
          },
          {
            "type": "command",
            "command": "bash -lc 'pnpm ut'",
            "timeout": 300,
            "statusMessage": "Running unit tests"
          }
        ]
      }
    ]
  }
}
